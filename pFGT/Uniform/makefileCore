
INCLUDE = ${PETSC_INCLUDE} 

LIBS = ${PETSC_LIB} 

all : runUniform

%.o: %.$(CEXT)
	$(PCC) -c $(CFLAGS) $(INCLUDE) $< -o $@

runUniform : ./runUniform.o ./pfgtUtils.o 
	$(PCC) $(CFLAGS) $^ -o $@ $(LIBS) 

clean :
	rm -rf runUniform
	rm -rf *.o


